[package]
name         = "annex-core"
description  = "Shared types, config, errors and pipeline glue for Annex."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
# M0 needs nothing beyond std. Wire these in as the milestones land:
#   serde, serde_json  -> when protocol.rs starts crossing the wire  (M3)
#   thiserror          -> when error.rs grows past a hand-written Display
