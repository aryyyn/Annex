[package]
name         = "annex-virtual-display"
description  = "Creates a macOS virtual monitor. The only crate touching private Apple APIs."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true

[dependencies]
annex-core = { workspace = true }
objc2.workspace = true
objc2-core-foundation.workspace = true
objc2-foundation.workspace = true

# Wired in at M0. These stay confined to this crate by design: see rule 3 in
# CLAUDE.md and section 5.1 of the design doc. The dispatch queue the descriptor
# needs comes from one extern "C" in ffi.rs rather than a dispatch2 dependency,
# since it is handed straight to Objective-C as an opaque pointer.
